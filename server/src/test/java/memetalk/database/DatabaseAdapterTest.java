package memetalk.database;

// import memetalk.ConfigReader;
// import memetalk.ConfigReader.DeploymentEnvironment;
import org.junit.Test;

public class DatabaseAdapterTest {
    @Test
    public void testSelectSucceed() {
        /*
        // We should either (1) mock/fake the database or (2) setup a test psql
        // environment for test. We shouldn't assume the test is ran with a
        // valid development environment.
        ConfigReader configReader = new ConfigReader(DeploymentEnvironment.DEV);
        try {
            DatabaseAdapter databaseAdapter = new DatabaseAdapter(configReader);
            databaseAdapter.dummySelect();
        } catch (Exception e) {
        } */
    }
}
